# V16.2 Build Fix

Fixed the TypeScript errors reported by the V16.1/V15 working tree.

## Fixes

1. Restored coaching-domain types consumed by `src/coachingEngine.ts`:
   - `ProgressionCriteria`
   - `ProgressionEvaluation`
   - `ReadinessAnalysis`
   - `SidePerformance`
   - `BodyweightPerformance`
   - `TrainingRole`, `TrainingPriority`, `BandMode`
2. Preserved `WorkoutLog.variantName` from the previous-exposure fix.
3. Added `criteria`, training role and priority fields without removing existing types.
4. Changed `proposeTargetProgression()` to return the draft form of `CoachProposal` (`Omit<CoachProposal, "id" | "date">`), because IDs/dates are generated by `saveCoachProposal()`.
5. Included `src/coachingEngine.ts` so the coaching engine and its domain types are part of the delivered project.

## Verification

The source-level fixes were applied. A clean dependency install could not be completed in the build environment because npm dependency download timed out, so a final `npm run build` is still required on the development machine after `npm ci`/`npm install`.
