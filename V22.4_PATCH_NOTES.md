# V22.4 — Coach runtime + UI hardening

- Simple greetings bypass AI/tool pipeline entirely.
- Deterministic fallback no longer labels local facts as OpenAI tool calls.
- Coach chat panel has min-width/overflow guards to prevent horizontal layout breakage.
- No changes to training prescriptions or periodization logic.
